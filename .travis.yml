language: php

php:
  - 5.6
  - 7.0
  - hhvm
  - nightly

before_script:
  - composer install --no-interaction
  - composer dump-autoload --optimize

script:
  - mkdir -p ./build/logs
  - ./vendor/bin/phpunit

after_script:
  - php ./vendor/bin/coveralls -v
